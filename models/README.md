# Models

Models are trained at runtime by the FastAPI backend using scikit-learn RandomForestClassifier.

No pretrained model or external dataset is committed. This is intentional because every season is synthetic and generated from a new random seed.
