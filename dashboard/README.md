# Dashboard

The dashboard implementation is in `frontend/`.

Pages included: Dashboard, Simulator, Classification, Growth Stage, Analytics, Water Advisory, and Settings. The UI includes dark mode, charts, confusion matrix heatmaps, noise controls, cloud controls, crop/plot selection, and season animation.
